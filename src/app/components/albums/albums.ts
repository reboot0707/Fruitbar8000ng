import { Component } from '@angular/core';
import { RouterLink } from "@angular/router";

@Component({
  imports: [RouterLink],
  selector: 'app-albums',
  styleUrl: './albums.css',
  templateUrl: './albums.html',
})
export class Albums {}
